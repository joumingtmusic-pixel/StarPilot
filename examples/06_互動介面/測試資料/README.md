# 🎨 互動介面測試資料

這個資料夾包含用於練習建立互動介面的測試數據和範例程式。

---

## 📊 測試資料

### sample_data.csv
- **用途**：練習建立資料上傳和展示介面
- **記錄數**：7 筆銷售資料
- **欄位**：日期、產品、數量、金額、地區、業務、客戶類型

---

## 🎯 練習建議

### 初級練習：檔案上傳介面
```
用 Copilot Chat 說：
「建立一個 Streamlit 介面
 讓使用者上傳 CSV 檔案
 並顯示前 10 筆資料」
```

### 中級練習：互動式篩選
```
「在介面上加上：
 - 地區選擇器（多選）
 - 日期範圍選擇器
 - 圖表會根據篩選條件即時更新」
```

### 高級練習：完整儀表板
```
「建立一個銷售儀表板
 包含：
 - 側邊欄篩選條件
 - 關鍵數字卡片（總銷售額、訂單數）
 - 多種圖表（長條圖、折線圖、圓餅圖）
 - 資料匯出功能」
```

---

## 💡 Streamlit 快速指令

```bash
# 啟動應用
streamlit run app.py

# 自動開啟瀏覽器
streamlit run app.py --server.headless false

# 指定 Port
streamlit run app.py --server.port 8502
```

---

## 🌟 範例程式碼片段

### 最簡單的開始
```python
import streamlit as st
import pandas as pd

st.title("我的第一個 Streamlit 應用")

uploaded_file = st.file_uploader("上傳 CSV 檔案")
if uploaded_file:
    df = pd.read_csv(uploaded_file)
    st.dataframe(df)
```

### 加上圖表
```python
st.line_chart(df['金額'])
st.bar_chart(df['數量'])
```

### 加上篩選
```python
selected_region = st.multiselect("選擇地區", df['地區'].unique())
filtered_df = df[df['地區'].isin(selected_region)]
st.dataframe(filtered_df)
```

---

記住：**好的介面讓工具更容易被使用！** 🎨
